<%= form_for(@seller_user) do |f| %>
  <% if @seller_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@seller_user.errors.count, "error") %> prohibited this seller_user from being saved:</h2>

      <ul>
      <% @seller_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
